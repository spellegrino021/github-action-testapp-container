name: Trigger auto deployment for name2342

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/name2342-AutoDeployTrigger-00ff4b59-2495-4e00-b480-bc05c7bb600b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.NAME2342_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.NAME2342_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.NAME2342_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryb6v72qek6ssog.azurecr.io
          registryUsername: ${{ secrets.NAME2342_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.NAME2342_REGISTRY_PASSWORD }}
          containerAppName: name2342
          resourceGroup: next13onswab10b
          imageToBuild: containerregistryb6v72qek6ssog.azurecr.io/name2342:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


