name: Trigger auto deployment for container-app-bxdpftwxfmqmw

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/container-app-bxdpftwxfmqmw-AutoDeployTrigger-d3fda622-29e6-46b2-904c-671e361b2696.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPBXDPFTWXFMQMW_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPBXDPFTWXFMQMW_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPBXDPFTWXFMQMW_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistry7chrxv336bxws.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPBXDPFTWXFMQMW_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPBXDPFTWXFMQMW_REGISTRY_PASSWORD }}
          containerAppName: container-app-bxdpftwxfmqmw
          resourceGroup: githubactiontestappcontainer18990
          imageToBuild: containerregistry7chrxv336bxws.azurecr.io/container-app-bxdpftwxfmqmw:${{ github.sha }}
          


